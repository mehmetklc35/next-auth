/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "s.gravatar.com",     // gravatar avatarları
      "cdn.auth0.com",      // auth0 avatarları
    ],
  },
};

export default nextConfig;

